# Non-sensitive configuration (Safe to commit to GitHub)
# The API keys and tokens are NOT stored here. They are securely injected by Kubernetes.
GRAFANA_URL=https://prometheus-prod-43-prod-ap-south-1.grafana.net/api/prom/push
GRAFANA_USER=3007636
SYNC_INTERVAL=3600
